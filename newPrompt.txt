# GOAL
Proje, JSON Server üzerinde çalışan fake bir API’ye test otomasyonu eklemek için hazırlanmıştır.  
Tüm endpoint’ler Swagger (OpenAPI) üzerinden alınacaktır.  
Testler TypeScript + Mocha + Chai kullanılarak yazılacaktır.  
API istekleri **ApiService** üzerinden yapılacaktır.

---

# TASKS

1. 🔀 **Branch Oluştur**
   - Yeni bir branch oluştur:
     git checkout -b `ai-<tool-name>-<YYYYMMDD-HHmm>`. (tool-name'den kastımız: cursor, windsurf, copilot, junie vb.)
     Örnek:
     ```powershell
     git checkout -b ("ai-" + "<tool-name>" + "-" + (Get-Date -Format "yyyyMMdd-HHmm"))
     ```
   - Tüm işlemleri bu branch üzerinde gerçekleştir.
   - ⚠️ **Commit veya push yapma.**  
     Tüm testler başarıyla geçince yalnızca “BİTTİ” diyerek işlemi sonlandır.

---

2. 🔍 **Kod Analizi**
   - `fake-api-test` klasörü altındaki mevcut CRUD testlerini incele.  
   - Bu testlerdeki `expect` bloklarının swagger’daki `response.data` yapısına göre yazıldığını fark et.  
   - Kodun genel test mantığını, import düzenini, test yazım stilini ve **şablon yapısını** kavra.  
   - CRUD testlerinde kullanılan test bloğu formatı **referans şablondur**.  
   - Yeni yazılacak tüm testler bu CRUD şablonuna **birebir sadık kalacaktır** (biçim, sıralama, `expect` tarzı dahil).

---

3. 📘 **Swagger Verisini Oku**
   - Swagger'ın güncel halini elde etmek için `npm run openapi:fetch` komutunu çalıştır. Bunu yaptığında güncel swagger spesifikasyonu proje root'undaki openapi.json dosyasında yer alacaktır.
   - Bu dosyayı doğrudan oku.  
   - Tüm endpoint’leri, HTTP metodlarını, request parametrelerini ve response yapılarını analiz et.

---

4. ⚙️ **Servis Şablonlarını İncele**
   - `src/api/fakeApi` klasöründeki mevcut servis dosyalarını tara.  
   - Servis fonksiyonlarının nasıl tanımlandığını, hangi HTTP methodlarını kullandığını ve nasıl değer döndürdüğünü incele.  
   - Tüm API istekleri şu yapı üzerinden yapılacak:
     ```ts
     await ApiService.getInstance().instance.<method>('/endpoint');
     ```
     > Burada `ApiService` zaten `http://localhost:8000` baseURL’ünü içerir.  
     > Testlerde veya servislerde sadece endpoint kısmı (`/users`, `/posts` vb.) yazılmalıdır.
   - Gerekiyorsa swagger’daki endpoint’lere uygun **yeni metotlar** oluştur.

---

5. 🧩 **Eksik Endpointler için Kod Yaz**
   - Swagger’da tanımlı olup proje içinde test veya servis dosyası bulunmayan endpointler için:
     - Servis dosyası oluştur: `src/api/fakeApi/specialEndpointService.ts`
     - Test dosyası oluştur: `fake-api-test/specialEndpoint.ts`
   - Endpoint’lere uygun şekilde request işlemlerini bu servis dosyalarında tanımla.  
   - Gerekirse servis tarafına **ek metotlar** yazılabilir.

---

6. 🧪 **Testleri Yaz**
   - İlk olarak CRUD (Create, Read, Update, Delete) işlemlerine ait endpoint testlerini yaz.  
   - CRUD testleri tamamlanıp tümü başarıyla geçtiğinde, senaryosal veya özel amaçlı endpoint testlerine (örneğin `/special`, `/custom`, `/auth` vb.) geç.  
   - Bu sıralamayı **kesinlikle** koru. Önce CRUD → sonra senaryosal endpointler yazılmalı.
   - Swagger’daki **tüm endpointler** için test oluştur (endpoint sayısı kadar).  
   - Testler 5’erli gruplar halinde yazılacak ve çalıştırılacak:
     1. İlk 5 testi yaz ve çalıştır.  
     2. Eğer tümü **pass** geçerse sonraki 5 teste geç.  
     3. Eğer herhangi biri **fail** olursa, önce başarısız testleri düzelt ve tekrar dene.  
     4. Tüm testler sırasıyla geçtikçe diğer gruba geç.  
   - Sonunda swagger’daki endpoint sayısı ile yazılmış test sayısı eşitlendiğinde işlemi tamamla.

   - ⚠️ **KURALLAR:**
     - Her test **tek tek** `it()` bloğu içinde yazılacak.  
     - **Kesinlikle** for, foreach, map veya benzeri döngü tabanlı otomatik test oluşturma yapılmayacak.  
     - Her endpoint için **ayrı bir `it()` tanımı** bulunacak.  
     - `it()` başlık formatı ZORUNLU: `<METHOD> <PATH> - <expectation statement>` olmalıdır.  
       - Dinamik segmentler placeholder ile yazılmalıdır: `/users/{id}`, `/orders/{id}` gibi; kesinlikle `/users/1` gibi somut değer kullanılmamalı.  
       - Örnekler:  
         - `GET /users/{id} - should return 200`  
         - `POST /users - creates new user with id`  
         - `DELETE /orders/{id} - removes order (200)`  
     - CRUD testlerindeki test yapısı, `describe` – `it` sırası, `expect` biçimi, `async` yapısı ve `await` kullanımı birebir korunacak.  
     - `it` ile test tanımlanırken **servis metoduna asla parametre yollanmayacak.**  
     - Eğer bir `data` gerekiyorsa bu `data`, **servis metodunun içinde** tanımlanacak.  
     - Servis içindeki istekler:
       ```ts
       await ApiService.getInstance().instance.<method>('/endpoint');
       ```
       biçiminde olacak.
     - Gerekirse servis tarafına **ek yardımcı metotlar** eklenebilir.

   - Testlerde kontrol sırası şu şekilde olacak:
     1. İlk olarak `response.status` değeri doğru mu (örneğin 200, 201 vb.) kontrol edilecek.  
     2. Daha sonra `response.data` içeriği `expect` bloklarıyla kontrol edilecek.  
     3. Eğer istekte bir `id` alanı gönderildiyse ve response.data içinde de ilgili `id` alanı varsa, bu iki ID’nin eşleşip eşleşmediği kontrol edilecek.  
        - Eğer ID’ler eşleşmiyorsa test **false (fail)** dönmeli.  
        - Eğer ID’ler eşleşiyorsa test normal akışında devam etmeli.  
     4. Nested (iç içe) objeler varsa:
        - Field tipleri (`string`, `number`, `array`, `object` vb.) doğrulanacak.  
        - Mantıksal doğrulamalar (`id > 0`, `count >= 0`, `name` boş olmamalı vb.) yapılacak.  
        - Array’ler varsa `length > 0` kontrolü yapılacak.  
        - Tarih formatı olan alanlarda (`createdAt`, `updatedAt`) uygun format olup olmadığı doğrulanacak.

   - CRUD testlerindeki kod formatı, import yapısı ve blok düzeni **zorunlu referans** olarak kabul edilecek.

   - 💥 **HER TESTE ÖZEL DETAYLI EXPECTLER HAZIRLANACAK!**
     - Swagger’daki `response.data` şemasına göre her endpoint için ayrı ayrı detaylı `expect` blokları yaz.  
     - Her field için tipi ve olası değeri kontrol et.  
     - Eğer bir endpoint nested veri döndürüyorsa, alt alanlarını da `expect` ile doğrula.  
     - Sayısal alanlar için mantıksal koşullar (`> 0`, `>= 0`) kontrol edilecek.  
     - Boolean alanlar için doğru/yanlış değerler test edilecek.  
     - Dizi dönen response’larda her elemanın yapısı doğrulanacak.  
     - Response yapısı swagger’daki ile birebir uyuşmalı.

---

7. ▶️ **Testleri Çalıştır**
   - Testleri şu komutla çalıştır:
     ```
     yarn run runner
     ```
   - 5’erli test gruplarını sırayla yürüt.  
   - Eğer testlerden biri başarısız olursa önce o testleri düzeltip tekrar çalıştır.  
   - Tüm testler başarılı olduğunda diğer gruba geç.

---

8. ✅ **Tamamlanma**
   - Swagger’daki toplam endpoint sayısı ile yazılmış test sayısı **birebir eşit** olmalı.  
   - Tüm testler başarıyla geçmiş olmalı.  
   - **Commit veya push yapma.**
   - Son olarak şu çıktıyı ver:
     ```
     BİTTİ
     ```

---

# TECHNOLOGIES
TypeScript · Mocha · Chai · JSON Server · Swagger (OpenAPI)  
Tüm istekler `ApiService.getInstance().instance` üzerinden gönderilecektir.

---

# NOTES
- `chai.expect` bloklarını **detaylı, kapsamlı ve spesifik** biçimde kullan.  
- Her endpoint testinde **kendine özgü** expect kontrolleri yer almalı.  
- CRUD testlerindeki biçim, import düzeni ve test yapısına %100 sadık kal.  
- Döngü ile test oluşturmak **kesinlikle yasak.**  
- Swagger’daki response şemalarından otomatik tip çıkarımı yapılabilir, ancak expect blokları elle yazılmalı.  
- Kodun temiz, düzenli ve anlaşılır olmasına dikkat et.
